<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ses1grp6.dbsystemandroid">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        android:fullBackupContent="@xml/backup_descriptor"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".donor.DonorWizard" />
        <activity android:name=".common.ApplicationActivity"></activity>
        <activity android:name=".common.ListingActivity" />
        <activity android:name=".charity.CharityWizard"></activity>
        <activity android:name=".util.simpleResult.SimpleResultActivity" />

        <activity android:name=".donor.ProfileActivity" />
        <activity android:name=".launch.LoginChoiceActivity" />
        <activity
            android:name=".DashboardActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".launch.RegistrationActivity" />
        <activity android:name=".launch.LoginActivity" />
        <activity android:name=".launch.LauncherActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>
